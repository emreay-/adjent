{
  "name": "adjent-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Observability and advisory orchestration for AI work, with a human tray UI and headless agent interfaces.",
  "license": "MIT",
  "engines": {
    "node": ">=22.12"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "status": "pnpm --filter @adjent/cli start status",
    "watch": "pnpm --filter @adjent/cli start watch"
  }
}
