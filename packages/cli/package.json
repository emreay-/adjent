{
  "name": "@adjent/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adjent headless CLI: status | watch | statusline.",
  "bin": {
    "adjent": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@adjent/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.7.0",
    "vitest": "^5.0.0"
  },
  "license": "MIT"
}
